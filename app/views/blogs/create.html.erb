<h1>Create New Blog</h1>

<%= form_with(scope: :blog, url: blogs_path, local: true) do |form| %>

  <%= form.label :title %>
  <%= form.text_field :title %>

  <%= form.label :body %>
  <%= form.text_area :body %>

  <%= form.submit :save %>




<% end %>